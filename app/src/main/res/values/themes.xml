<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SiKarir" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="textFillColor">@color/black</item>
        <item name="backgroundColor">@color/purple_primary</item>
        <item name="backgroundTint">@color/white</item>
        <item name="textInputFilledStyle">@color/white</item>
        <!-- Navbar -->
        <item name="cardBackgroundColor">@color/purple_primary</item>
    </style>

    <style name="Theme.SiKarir" parent="Base.Theme.SiKarir" />
</resources>